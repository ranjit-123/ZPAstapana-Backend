package com.zpasthapana.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RestController;

import com.zpasthapana.pojo.MultiPartBodyRequest;


@RestController
public class SampleMultipart {

	/*
	 * @PostMapping(value = "importProject/{projectId}", consumes =
	 * "multipart/form-data") public ResponseEntity<T> receiveData(@ModelAttribute
	 * MultiPartBodyRequest request, @PathVariable int projectId) throws Exception {
	 * //return projectAndUserService.importProject(projectId, request); }
	 */
}
